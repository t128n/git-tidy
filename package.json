{
  "name": "@t128n/git-tidy",
  "version": "0.1.0",
  "description": "Git workspace organizer and cloning utility",
  "license": "MIT",
  "private": true,
  "scripts": {
    "version-packages": "changeset version && bun run ./scripts/sync-version.ts"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.0"
  }
}
